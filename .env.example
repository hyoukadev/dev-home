GITHUB_TOKEN=
# Optional: pull/run an image published by GitHub Actions.
# DEV_HOME_IMAGE=ghcr.io/<owner>/<repo>:latest
# Optional: override the host directory mounted at /workspace.
# DEV_HOME_HOST_WORKSPACE=/home/hyouka/item
# Optional Intel iGPU passthrough mode: auto, none, intel, or dri.
# DEV_HOME_GPU=auto
# Optional manual override for the Podman --device entry.
# DEV_HOME_DRI_DEVICE=/dev/dri:/dev/dri
